const {chromium}=require('C:/Users/ACER/AppData/Local/npm-cache/_npx/db89d7302a373f10/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({headless:true,args:['--use-angle=swiftshader','--enable-unsafe-swiftshader']});const p=await b.newPage();await p.addInitScript(()=>{const original=HTMLCanvasElement.prototype.getContext;HTMLCanvasElement.prototype.getContext=function(type,...args){if(String(type).includes('webgl'))return null;return original.call(this,type,...args)}});await p.goto('http://127.0.0.1:5789/tests/browser/globe.html?view=3d');await p.getByRole('button',{name:'Return to 2D map',exact:true}).waitFor({timeout:60000});assert(await p.getByRole('alert').isVisible());await p.getByRole('button',{name:'Return to 2D map',exact:true}).click({force:true});await p.waitForSelector('.leaflet-container',{timeout:60000});console.log('fallback ok');await b.close()})().catch(e=>{console.error(e);process.exit(1)})
